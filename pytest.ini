[pytest]
# Recolher testes apenas da pasta tests/ (testes unitarios reais).
# Evita que o pytest apanhe scripts manuais como o smoke_test.py do Lab 8,
# que nao sao testes pytest e executam codigo ao serem importados.
testpaths = tests
