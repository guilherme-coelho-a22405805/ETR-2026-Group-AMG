# Test dependencies — adicionado no grooming do Lab 14.
#
# NOTA (entrega final): a partir da entrega, o ficheiro principal de dependencias
# e o requirements.txt na raiz, que ja inclui app + testes. Este ficheiro mantem-se
# por compatibilidade com o registo do Lab 14 e lista apenas as dependencias de teste.
# Para instalar tudo de uma vez, usar:  pip install -r requirements.txt
pytest>=8.0
behave>=1.2.6
